function Functionpush() {
    alert("Are you sure about that");
    var answer = confirm("Really?") //Giving the confirm box to answer
    if(answer) //Doouble check to confirm
    window.location ="example.html" //If okay, then jump to next page
  }
